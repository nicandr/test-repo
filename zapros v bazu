SELECT MAX(tickets) as tiketi, max(calls) as calls,login,login_time FROM `logs`,`users` WHERE users.id=1 and users.id=logs.id group by logs.login UNION SELECT SUM(max_value),SUM(calls1),login,login_time FROM (select Max(tickets) as max_value,MAX(calls) as calls1,login,login_time from `logs` where logs.id=1 group by login) x
